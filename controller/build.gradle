dependencies {
    implementation project(':beatport-scraper')
    implementation project(':data')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}

test {
    useJUnitPlatform()
}

bootJar {
    enabled = true
}