plugins {
    id 'java'
    id 'org.springframework.boot' version '3.1.5'
    id 'io.spring.dependency-management' version '1.1.3'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    group = 'com.andrewsavich.djguesser'
    version = '1.0.0'

    sourceCompatibility = '17'

    repositories {
        mavenCentral()
    }

    test {
        useJUnitPlatform()
    }
    bootJar {
        enabled = false
    }

    dependencyManagement {
        imports {
            mavenBom "org.seleniumhq.selenium:selenium-java:4.15.0"
        }
    }

}

bootJar {
    enabled = false
}